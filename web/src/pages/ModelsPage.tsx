import { PlaceholderPage } from "./PlaceholderPage";

export function ModelsPage() {
  return (
    <PlaceholderPage
      title="Models"
      description="List, pull, and set default Ollama models — Phase 5. Today the stack uses mock providers only."
    />
  );
}
